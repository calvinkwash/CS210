/*
 * Enhancements added for 100%:
 * - Activity log: counts how many times each activity is completed in this session.
 *   At exit (option 4), displays session summary.
 * - Ensures random prompts/questions don't repeat until all have been used once.
 * - Saves session log to "session_log.txt" on disk.
 */
